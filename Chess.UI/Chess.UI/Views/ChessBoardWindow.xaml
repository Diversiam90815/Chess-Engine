<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Chess.UI.Views.ChessBoardWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Chess.UI.Views"   
    xmlns:vm="using:Chess.UI.Board"
    xmlns:converter="using:Chess.UI.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Chess"
    >

    <StackPanel x:Name="RootPanel" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">

        <Border Style="{StaticResource BackgroundBorder}">
            <Grid x:Name="BoardGrid" Height="600" Width="600" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="30" XYFocusKeyboardNavigation="Enabled">
                <Grid.Background>
                    <ImageBrush ImageSource="{x:Bind _viewModel.BoardBackgroundImage, Mode=OneWay}"/>
                </Grid.Background>

                <ItemsRepeater x:Name="ChessBoardRepeater" ItemsSource="{x:Bind _viewModel.Board, Mode=TwoWay}">
                    <ItemsRepeater.Layout>
                        <UniformGridLayout MinItemHeight="75" MinItemWidth="75" MaximumRowsOrColumns="8" />
                    </ItemsRepeater.Layout>

                    <ItemsRepeater.ItemTemplate >
                        <DataTemplate x:DataType="vm:BoardSquare" >
                            <Grid Tapped="ChessPiece_Clicked" DataContext="{x:Bind}" Background="{x:Bind BackgroundBrush, Mode=OneWay}">
                                <Image Source="{x:Bind PieceImage, Mode=OneWay}" Stretch="Uniform" />
                            </Grid>
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>

                </ItemsRepeater>
            </Grid>

        </Border>

        <Border Style="{ThemeResource BackgroundBorder}">

            <Grid x:Name="InfoGrid" Margin="30" VerticalAlignment="Top">

                <StackPanel Orientation="Vertical">

                    <StackPanel x:Name="CurrentPlayerSP" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" Height="Auto" Margin="16" Orientation="Horizontal" CornerRadius="8" Visibility="{x:Bind converter:Converter.BoolToCollapsed(_viewModel.IsMultiplayerGame), Mode=OneTime}" >
                        <TextBlock Text="Current Player" HorizontalAlignment="Center" TextAlignment="Center" VerticalAlignment="Center" FontSize="24" Margin="8"/>
                        <Image Source="{x:Bind converter:Converter.PlayerToPawnImage(_viewModel.CurrentPlayer), Mode=OneWay}" Style="{StaticResource CurrentPlayerImageStyle}"/>
                    </StackPanel>


                    <StackPanel x:Name="CurrentPlayerMP" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" Height="Auto" Margin="16" Orientation="Horizontal" CornerRadius="8" Visibility="{x:Bind converter:Converter.BoolToVisibility(_viewModel.IsMultiplayerGame), Mode=OneTime}" >
                        <TextBlock Text="Local Player" HorizontalAlignment="Center" TextAlignment="Center" VerticalAlignment="Center" FontSize="24" Margin="8"/>
                        <Image Source="{x:Bind converter:Converter.PlayerToPawnImage(_viewModel._multiplayerViewModel.LocalPlayer), Mode=OneTime}" Style="{StaticResource CurrentPlayerImageStyle}"/>
                        <Ellipse Width="10" Height="10" Fill="{x:Bind converter:Converter.BoolToColor(_viewModel._multiplayerViewModel.IsLocalPlayersTurn), Mode=OneWay}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>

                    <Grid x:Name="Score" Width="160" Height="170" CornerRadius="8" Margin="16" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" HorizontalAlignment="Center">
                        <Grid.RowDefinitions>
                            <RowDefinition x:Name="ScoreTitle" Height="Auto"/>
                            <RowDefinition x:Name="BlackWhiteTitle" Height="Auto" />
                            <RowDefinition x:Name="Pieces" Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition x:Name="White" Width="80"/>
                            <ColumnDefinition x:Name="Black" Width="80"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Score" FontSize="24" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="White" FontSize="20" HorizontalAlignment="Left" Margin="8,0,0,0"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="Black" FontSize="20" HorizontalAlignment="Left" Margin="8,0,0,0"/>

                        <StackPanel Name="WhiteCapturedPieces" Grid.Row="2" Grid.Column="0" Orientation="Horizontal">
                            <StackPanel Name="WCapturedPiecesImages" Orientation="Vertical" HorizontalAlignment="Left">
                                <Image Source="{x:Bind _viewModel._scoreViewModel.CapturedBlackPawnImage}" Style="{StaticResource CapturedPieceImageStyle}"/>
                                <Image Source="{x:Bind _viewModel._scoreViewModel.CapturedBlackBishopImage}" Style="{StaticResource CapturedPieceImageStyle}"/>
                                <Image Source="{x:Bind _viewModel._scoreViewModel.CapturedBlackKnightImage}" Style="{StaticResource CapturedPieceImageStyle}"/>
                                <Image Source="{x:Bind _viewModel._scoreViewModel.CapturedBlackRookImage}" Style="{StaticResource CapturedPieceImageStyle}"/>
                                <Image Source="{x:Bind _viewModel._scoreViewModel.CapturedBlackQueenImage}" Style="{StaticResource CapturedPieceImageStyle}"/>
                            </StackPanel>

                            <StackPanel Name="WNumCapturedPiece" Orientation="Vertical" HorizontalAlignment="Center">
                                <TextBlock Text="{x:Bind _viewModel._scoreViewModel.WhiteCapturedPawn, Mode=OneWay}" Style="{StaticResource CapturedPieceNumberStyle}" />
                                <TextBlock Text="{x:Bind _viewModel._scoreViewModel.WhiteCapturedBishop, Mode=OneWay}" Style="{StaticResource CapturedPieceNumberStyle}" />
                                <TextBlock Text="{x:Bind _viewModel._scoreViewModel.WhiteCapturedKnight, Mode=OneWay}" Style="{StaticResource CapturedPieceNumberStyle}" />
                                <TextBlock Text="{x:Bind _viewModel._scoreViewModel.WhiteCapturedRook, Mode=OneWay}" Style="{StaticResource CapturedPieceNumberStyle}" />
                                <TextBlock Text="{x:Bind _viewModel._scoreViewModel.WhiteCapturedQueen, Mode=OneWay}" Style="{StaticResource CapturedPieceNumberStyle}" />
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Name="BlackCapturedPieces" Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                            <StackPanel Name="BCapturedPiecesImages" Orientation="Vertical" HorizontalAlignment="Left">
                                <Image Source="{x:Bind _viewModel._scoreViewModel.CapturedWhitePawnImage}" Style="{StaticResource CapturedPieceImageStyle}"/>
                                <Image Source="{x:Bind _viewModel._scoreViewModel.CapturedWhiteBishopImage}" Style="{StaticResource CapturedPieceImageStyle}"/>
                                <Image Source="{x:Bind _viewModel._scoreViewModel.CapturedWhiteKnightImage}" Style="{StaticResource CapturedPieceImageStyle}"/>
                                <Image Source="{x:Bind _viewModel._scoreViewModel.CapturedWhiteRookImage}" Style="{StaticResource CapturedPieceImageStyle}"/>
                                <Image Source="{x:Bind _viewModel._scoreViewModel.CapturedWhiteQueenImage}" Style="{StaticResource CapturedPieceImageStyle}"/>
                            </StackPanel>

                            <StackPanel Name="BNumCapturedPiece" Orientation="Vertical" HorizontalAlignment="Center">
                                <TextBlock Text="{x:Bind _viewModel._scoreViewModel.BlackCapturedPawn, Mode=OneWay}" Style="{StaticResource CapturedPieceNumberStyle}" />
                                <TextBlock Text="{x:Bind _viewModel._scoreViewModel.BlackCapturedBishop, Mode=OneWay}" Style="{StaticResource CapturedPieceNumberStyle}" />
                                <TextBlock Text="{x:Bind _viewModel._scoreViewModel.BlackCapturedKnight, Mode=OneWay}" Style="{StaticResource CapturedPieceNumberStyle}" />
                                <TextBlock Text="{x:Bind _viewModel._scoreViewModel.BlackCapturedRook, Mode=OneWay}" Style="{StaticResource CapturedPieceNumberStyle}" />
                                <TextBlock Text="{x:Bind _viewModel._scoreViewModel.BlackCapturedQueen, Mode=OneWay}" Style="{StaticResource CapturedPieceNumberStyle}" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>

                    <Grid x:Name="MoveHistory" VerticalAlignment="Center" Width="220" Height="220" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="8" Margin="16" HorizontalAlignment="Center">
                        <Grid.RowDefinitions>
                            <RowDefinition x:Name="Title" Height="40"/>
                            <RowDefinition x:Name="Moves" Height="165"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Move History" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24"/>

                        <!-- Moves with Wrapping Columns -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <ItemsControl x:Name="MovesColumns" ItemsSource="{x:Bind _viewModel._moveHistoryViewModel.MoveHistoryColumns, Mode=OneWay}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical" Margin="14,0">
                                            <!-- Each Column -->
                                            <ItemsControl ItemsSource="{Binding}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding}" Width="40" FontSize="14" HorizontalAlignment="Left" TextAlignment="Center" Margin="2"/>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>

                    <StackPanel x:Name="Buttons" Orientation="Horizontal" Spacing="12" Margin="0,16" HorizontalAlignment="Center">
                        <Button x:Name="UndoMove" Content="Undo" Click="UndoMove_Click" Style="{StaticResource GameButtonStyle}" Visibility="{x:Bind converter:Converter.BoolToCollapsed(_viewModel.IsMultiplayerGame), Mode=OneWay}"/>
                        <Button x:Name="ResetGame" Content="Reset" Click="ResetGame_Click" Style="{StaticResource GameButtonStyle}" Visibility="{x:Bind converter:Converter.BoolToCollapsed(_viewModel.IsMultiplayerGame), Mode=OneWay}"/>
                        <Button x:Name="EndGame" Content="End" Click="EndGame_Click" Style="{StaticResource GameButtonStyle}"/>
                    </StackPanel>

                </StackPanel>
            </Grid>
        </Border>

    </StackPanel>
</Window>
